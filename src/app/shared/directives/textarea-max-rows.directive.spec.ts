import { TextareaMaxRowsDirective } from './textarea-max-rows.directive';

describe('TextareaMaxRowsDirective', () => {
  it('should create an instance', () => {
    const directive = new TextareaMaxRowsDirective();
    expect(directive).toBeTruthy();
  });
});
