export enum Operator {
   CONTAINS = 'contains',
   LESS_THAN = '<',
   LESS_THAN_OR_EQUAL = '<=',
   EQUAL = '=',
   NOT_EQUAL = '≠',
   GREATER_THAN_OR_EQUAL = '>=',
   GREATER_THAN = '>',
   NOT_EMPTY = 'not empty'
}
